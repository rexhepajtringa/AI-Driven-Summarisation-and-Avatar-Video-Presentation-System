import React from "react";

const HomePage: React.FC = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}>
      <h1>Welcome!</h1>
    </div>
  );
};

export default HomePage;
